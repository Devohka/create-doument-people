


export const inpersonEl = state => state.inPerson.inperson;

export const customerEl = state => state.inPerson.customer;

export const basisEl = state => state.inPerson.basis;

export const termEl = state => state.inPerson.term;

export const placeEl = state => state.inPerson.place;

export const codeEl = state => state.inPerson.code;

export const showDEl = state => state.inPerson.showD;




export const productEl = state => state.product.product;

export const showModalEl = state => state.product.show;



export const showProviderEl = state => state.provider.show;

export const customerProviderEl = state => state.provider.customer;

export const providerEl = state => state.provider.provider;
